# BY19Code T01-T13 完成 - 准备测试

## ✅ 已完成的任务

- [x] T01-T07：LLM 适配层和配置管理（验收节点 A：能说话）
- [x] T08：文件操作模块
- [x] T09：命令执行沙箱
- [x] T10：工具定义与注册
- [x] T11：对话引擎
- [x] T12：上下文管理器
- [x] T13：CLI 交互界面

## 📋 启动前检查清单

### 1. 依赖安装 ✅
```bash
pip install -e .
```

### 2. 配置文件 ✅
- `config.json` - 已创建
- `.env` - 已创建

### 3. API Key 配置 ⚠️

**当前状态：**
- ✅ DeepSeek API Key 已配置
- ⚠️ Claude API Key 未配置

**需要你做的：**

编辑 `.env` 文件，填入 Claude API Key：

```bash
BY19CODE_CLAUDE_API_KEY=sk-ant-你的真实key
```

如果使用中转站，还需要配置：
```bash
BY19CODE_CLAUDE_BASE_URL=https://你的中转站地址
```

### 4. 数据库 ✅
- 数据库路径：`%USERPROFILE%\.by19code\by19code.db`
- 初始化：自动完成

## 🚀 启动 CLI

配置好 API Key 后，运行：

```bash
python -m by19code.main
```

或者指定项目目录：

```bash
python -m by19code.main --project D:\MyProject
```

## 📝 测试清单（验收节点 B+C）

启动 CLI 后，按顺序测试以下功能：

### 1. 基础对话
```
> 你好
```
**预期**：AI 流式回复

### 2. 文件创建
```
> 帮我创建一个 hello.py，打印 hello world
```
**预期**：
- 显示 [工具] 调用: write_file
- 显示 [文件] 创建成功
- 文件被创建

### 3. 文件读取
```
> 读取 hello.py 的内容
```
**预期**：显示文件内容

### 4. 文件编辑
```
> 把 hello world 改成 hello BY19Code
```
**预期**：
- 显示 [工具] 调用: edit_file
- 文件被修改

### 5. 目录列表
```
> 显示当前目录有哪些文件
```
**预期**：显示目录树

### 6. 命令执行
```
> 运行 python hello.py
```
**预期**：输出 "hello BY19Code"

### 7. 模型切换（可选）
```
> /switch deepseek
> 你好，你是什么模型？
```
**预期**：切换到 DeepSeek 并回复

### 8. 上下文统计
```
> /stats
```
**预期**：显示消息数、token 数、使用率

### 9. 费用查看
```
> /cost
```
**预期**：显示累计费用和 token 用量

### 10. 安全测试
```
> 读取 C:\Windows\System32\drivers\etc\hosts
```
**预期**：被拒绝，显示 [错误] 文件路径超出项目范围

### 11. 退出
```
> /exit
```

## ⚠️ 已知问题

### 1. 中文显示乱码
**原因**：Windows cmd.exe 默认 GBK 编码

**解决方案**：
- 使用 Windows Terminal（推荐）
- 或在 cmd 中运行：`chcp 65001`

### 2. Python 3.14 警告
```
DeprecationWarning: 'asyncio.WindowsSelectorEventLoopPolicy' is deprecated
```
**说明**：不影响功能，可以忽略

## 📊 测试结果记录

测试完成后，请记录：

- [ ] 基础对话：正常 / 异常
- [ ] 文件创建：正常 / 异常
- [ ] 文件读取：正常 / 异常
- [ ] 文件编辑：正常 / 异常
- [ ] 目录列表：正常 / 异常
- [ ] 命令执行：正常 / 异常
- [ ] 模型切换：正常 / 异常 / 未测试
- [ ] 上下文统计：正常 / 异常
- [ ] 费用查看：正常 / 异常
- [ ] 安全测试：正常 / 异常

## 🎯 下一步

测试通过后：
- [ ] 标记验收节点 B+C 完成
- [ ] 继续 T14：Git 操作模块
- [ ] 继续 T15：集成测试
- [ ] 最终验收节点 D

## 💡 故障排查

### 问题：启动失败
1. 检查 API Key 是否正确
2. 检查网络连接
3. 查看日志：`by19code.log`

### 问题：API 调用失败
1. 检查 `.env` 文件中的 API Key
2. 如果使用中转站，检查 base_url
3. 测试网络：`ping api.anthropic.com`

### 问题：工具调用失败
1. 检查文件路径是否在项目目录内
2. 检查文件权限
3. 查看详细错误信息

---

**准备好了吗？配置 Claude API Key 后就可以开始测试了！**
